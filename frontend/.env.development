# 开发环境
# VITE_API_BASE_URL 留空，让所有 API 请求走相对路径，由 Vite 代理转发到后端
# 代理目标在 vite.config.ts 的 server.proxy 中配置
VITE_API_BASE_URL=
# Vite 代理转发的后端目标地址（仅开发环境使用）
VITE_PROXY_TARGET=http://127.0.0.1:8080
# B2 重构: 移除前端 HMAC 签名密钥 VITE_SIGN_SECRET
# 防重放改由后端签发一次性 token (getSeckillToken 接口), 前端不再持有任何共享密钥.
# 旧密钥 wnj-seckill-mall-hmac-sha256-sign-secret-2026 已废弃, 后端应同步移除 seckill.security.sign-secret 配置.
VITE_APP_TITLE=SeckillMall 秒杀商城
