/**
 * Cart 应用服务转换辅助 - VO/Entity ↔ DTO/Command 转换。
 *
 * <p>集中存放旧 VO/Entity 与新 API 层 DTO/Command 之间的转换方法，
 * 供 CartApplicationService 调用。
 *
 * @author wnj
 * @since Phase C.0
 */
package com.seckill.mall.cart.application.facade;