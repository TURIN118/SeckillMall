/**
 * Identity 持久化对象 - PO（Persistent Object）。
 *
 * <p>对应数据库表的 Java 对象，如 User、UserAddress、UserFavorite、LoginLog 等。
 * 仅在 infrastructure 层内部使用，不对外暴露。
 *
 * @author wnj
 * @since Phase I.0
 */
package com.seckill.mall.identity.infrastructure.entity;