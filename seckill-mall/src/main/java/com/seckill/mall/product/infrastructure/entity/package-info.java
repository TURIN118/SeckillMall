/**
 * Product 持久化对象 - PO（Persistent Object）。
 *
 * <p>对应数据库表的 Java 对象，如 Product、ProductSku、Category 等。
 * 仅在 infrastructure 层内部使用，不对外暴露。
 *
 * @author wnj
 * @since Phase P.0
 */
package com.seckill.mall.product.infrastructure.entity;