/**
 * Upload 应用层转换器 - VO ↔ DTO 转换。
 *
 * <p>包含 UploadApiConverter，集中存放旧 VO 与新 API 层 DTO 之间的转换方法，
 * 处理 UploadResultVO ↔ UploadResultDTO 字段映射。
 *
 * @author wnj
 * @since Phase U.0
 */
package com.seckill.mall.upload.application.facade;