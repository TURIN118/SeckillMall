spring:
  datasource:
    url: jdbc:mysql://localhost:3307/seckill_mall?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: abc123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        pool:
          max-active: 8
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: manual
        prefetch: 1
        concurrency: 3
        retry:
          enabled: false
  mail:
    host: smtp.qq.com
    port: 465
    username: turin118@qq.com
    password: mztzyzeusvsrdgcb
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          ssl:
            enable: true
          auth: true
          starttls:
            enable: true

jwt:
  secret: MySecretKeyForJwtHS256Algorithm2024SeckillMall
  access-token-expiration: 7200000
  refresh-token-expiration: 604800000

seckill:
  pay-timeout-minutes: 15
  rate-limit-per-user: 1
  rate-limit-per-ip: 30
  security:
    sign-secret: wnj-seckill-mall-hmac-sha256-sign-secret-2026
    replay-window-seconds: 60

logging:
  level:
    com.seckill.mall: DEBUG
