<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mall.mapper.CategoryMapper">

    <select id="selectByParentId" resultType="com.seckill.mall.entity.Category">
        SELECT id, name, parent_id, sort_order, icon_url, status,
               is_deleted, create_time, update_time
        FROM t_category
        WHERE parent_id = #{parentId}
          AND is_deleted = 0
        ORDER BY sort_order ASC
    </select>

</mapper>
