<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mall.identity.infrastructure.mapper.LoginLogMapper">

    <select id="selectByUserId" resultType="com.seckill.mall.identity.infrastructure.entity.LoginLog">
        SELECT id, user_id, login_ip, login_location, user_agent,
               login_result, fail_reason, create_time
        FROM t_login_log
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

</mapper>
