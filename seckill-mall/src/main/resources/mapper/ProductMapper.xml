<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mall.mapper.ProductMapper">

    <select id="selectProductPage" resultType="com.seckill.mall.entity.Product">
        SELECT id, name, description, original_price, stock, sales_count,
               category_id, images, main_image, detail_html, status,
               is_deleted, create_time, update_time
        FROM t_product
        WHERE is_deleted = 0
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY
        <choose>
            <when test="sortField == 'price'">original_price</when>
            <when test="sortField == 'sales'">sales_count</when>
            <when test="sortField == 'createTime'">create_time</when>
            <otherwise>create_time</otherwise>
        </choose>
        <choose>
            <when test="sortOrder == 'asc'">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>
    </select>

</mapper>
