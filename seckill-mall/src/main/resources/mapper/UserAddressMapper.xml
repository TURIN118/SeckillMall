<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mall.mapper.UserAddressMapper">

    <select id="selectByUserId" resultType="com.seckill.mall.entity.UserAddress">
        SELECT id, user_id, receiver_name, receiver_phone, province, city,
               district, detail_address, is_default,
               is_deleted, create_time, update_time
        FROM t_user_address
        WHERE user_id = #{userId}
          AND is_deleted = 0
        ORDER BY is_default DESC, update_time DESC
    </select>

</mapper>
