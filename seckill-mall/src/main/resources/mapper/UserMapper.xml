<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mall.mapper.UserMapper">

    <select id="findByUsername" resultType="com.seckill.mall.entity.User">
        SELECT id, username, password, phone, email, nickname, avatar_url,
               role, status, is_deleted, create_time, update_time
        FROM t_user
        WHERE username = #{username}
          AND is_deleted = 0
    </select>

    <select id="findByPhone" resultType="com.seckill.mall.entity.User">
        SELECT id, username, password, phone, email, nickname, avatar_url,
               role, status, is_deleted, create_time, update_time
        FROM t_user
        WHERE phone = #{phone}
          AND is_deleted = 0
    </select>

</mapper>
