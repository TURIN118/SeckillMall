# 秒杀抢购 401 Unauthorized 问题修复方案

> 生成时间：2026-08-05
> 问题现象：前端首页秒杀区域点击"立即抢购" → 弹出"网络错误"，后端返回 401 Unauthorized

---

## 一、问题根因总结（三层问题叠加）

### 根因1：ReplayProtectionFilter 拦截请求（最直接原因）

**文件**：`seckill-mall/src/main/java/com/seckill/mall/security/ReplayProtectionFilter.java`

该过滤器拦截所有 POST 请求到 `/api/v1/seckill/` 路径（第70-76行），要求三个防重放头：

| 必需头部 | 作用 | 前端是否携带 |
|----------|------|-------------|
| `X-Sign` | HMAC-SHA256 签名 | ❌ 未携带 |
| `X-Timestamp` | 请求时间戳 | ❌ 未携带 |
| `X-Nonce` | 防重放随机数 | ❌ 未携带 |

前端 `executeSeckill()` 和 `doSeckill()` 均未实现防重放签名逻辑，请求被直接拒绝返回 401。

### 根因2：ReplayProtectionFilter 的 401 响应缺少 CORS 头

**文件**：`ReplayProtectionFilter.java` 第125-131行

`ReplayProtectionFilter` 使用 `@Order(Ordered.HIGHEST_PRECEDENCE + 20)` 注册，执行顺序在 Spring Security 过滤链之前。当它拒绝请求时，响应不经过 Spring Security 的 `CorsFilter`，因此缺少 `Access-Control-Allow-Origin` 等 CORS 头。

浏览器对跨域请求严格执行 CORS 策略，没有 CORS 头的响应会被浏览器直接丢弃，前端 JavaScript 无法读取任何响应内容。这就是用户看到"网络错误"而非"401 未授权"的原因。

**执行顺序**：
```
ReplayProtectionFilter (@Order=HIGHEST_PRECEDENCE+20)  ← 最先执行
    ↓ 如果放行
Spring Security FilterChain (CorsFilter → JwtFilter → ...)  ← 后执行
```

### 根因3：请求直接发到后端端口而非前端代理端口

**文件**：`frontend/.env.development`

```
VITE_API_BASE_URL=http://localhost:8080
```

Axios 实例的 `baseURL` 为绝对地址 `http://localhost:8080`，所有 API 请求直接发到后端，完全绕过了 Vite 的开发代理（`vite.config.ts` 中配置的 `proxy`）。

---

## 二、完整因果链

```
VITE_API_BASE_URL=http://localhost:8080（绝对URL）
    → 请求跨域发到后端 8080
    → ReplayProtectionFilter 拦截（缺少 X-Sign/X-Timestamp/X-Nonce）
    → 返回 401 但不带 CORS 头（Filter 在 Security 之前执行）
    → 浏览器因 CORS 策略阻止前端读取响应
    → Axios 走进"网络异常"分支
    → 用户看到"网络错误"弹窗
```

---

## 三、前端执行流程（从点击到报错）

```
用户点击"立即抢购"
    ↓
Home.vue:132  @click.stop="handleSeckillBuy(item)"
    ↓
Home.vue:420  handleSeckillBuy(item)
    ├── 1. 检查 userStore.isLoggedIn（已登录，通过）
    ├── 2. 弹出 ElMessageBox.confirm 确认框
    └── 3. 用户确认后 → executeSeckill(item.id)
            ↓
        api/seckill.ts:51-53  executeSeckill()
            → post('/api/v1/seckill/{seckillId}/execute')
            → Axios 实例 baseURL = "http://localhost:8080"（来自 .env.development）
            → 请求拦截器自动添加 Authorization: Bearer <token>
            ↓
        实际发出请求：POST http://localhost:8080/api/v1/seckill/2084297480440176641/execute
            ⚠️ 只带了 Authorization 头，没有带 X-Sign / X-Timestamp / X-Nonce
            ↓
        后端返回 401（但缺少 CORS 头）
            ↓
        浏览器因跨域响应缺少 CORS 头，阻止前端读取响应
            ↓
        Axios 错误回调进入 else 分支 → ElMessage.error('网络异常，请检查连接')
            ↓
        用户看到"网络错误"弹窗
```

---

## 四、后端执行流程（从接收到拒绝）

```
请求到达 Servlet 容器
    ↓
【Filter 1】ReplayProtectionFilter（@Order(HIGHEST_PRECEDENCE + 20)）
    ├── 检查：是否为 POST 且 URI 以 /api/v1/seckill/ 开头（排除 /admin）→ ✅ 命中
    ├── 检查 X-Sign 头 → ❌ 不存在
    ├── 检查 X-Timestamp 头 → ❌ 不存在
    ├── 检查 X-Nonce 头 → ❌ 不存在
    └── reject() → 直接写入 401 响应，不带 CORS 头
        ↓
    ❌ 请求到此终止，永远不会进入 Spring Security 过滤链

（如果 ReplayProtectionFilter 放行，后续流程应该是：）
【Filter 2】Spring Security CorsFilter → 添加 CORS 头
【Filter 3】JwtAuthenticationFilter → 解析 Bearer Token，设置 SecurityContext
【Filter 4】SecurityConfig 权限校验 → /api/v1/seckill/{id}/execute 需要认证
【Controller】SeckillController.execute() → securityUtils.getCurrentUserId() → 执行秒杀
```

---

## 五、修复方案

### 方案A：全部修复（推荐）—— 修复三层问题

| 层次 | 修复内容 | 涉及文件 |
|------|---------|---------|
| 前端-防重放签名 | 在秒杀下单 API 调用前，生成 X-Sign、X-Timestamp、X-Nonce 三个头 | `frontend/src/api/seckill.ts`、新增 `frontend/src/utils/replayProtection.ts` |
| 后端-CORS头 | 在 ReplayProtectionFilter.reject() 中补充 CORS 响应头 | `seckill-mall/.../security/ReplayProtectionFilter.java` |
| 前端-Vite代理 | .env.development 改为 VITE_API_BASE_URL=（空） | `frontend/.env.development` |

#### 5.1 前端防重放签名实现

后端签名算法（ReplayProtectionFilter 第112-114行）：
```java
String payload = timestamp + nonce + request.getRequestURI();
String expected = hmacSha256Hex(signSecret, payload);
```

前端需实现对应逻辑（新建 `frontend/src/utils/replayProtection.ts`）：

```typescript
import CryptoJS from 'crypto-js'

/** 签名密钥，需与后端 seckill.security.sign-secret 配置一致 */
const SIGN_SECRET = '此处填入与后端一致的密钥'

/**
 * 生成防重放请求头
 * @param uri 请求 URI（如 /api/v1/seckill/123/execute）
 */
export function generateReplayHeaders(uri: string): Record<string, string> {
  const timestamp = Date.now().toString()
  const nonce = crypto.randomUUID()
  const payload = timestamp + nonce + uri
  const sign = CryptoJS.HmacSHA256(payload, SIGN_SECRET).toString(CryptoJS.enc.Hex)
  return {
    'X-Sign': sign,
    'X-Timestamp': timestamp,
    'X-Nonce': nonce
  }
}
```

修改 `frontend/src/api/seckill.ts`：

```typescript
import { generateReplayHeaders } from '@/utils/replayProtection'

/** 执行秒杀（带防重放签名） */
export function executeSeckill(seckillId: number | string): Promise<Result<SeckillResultVO>> {
  const uri = `/api/v1/seckill/${seckillId}/execute`
  return post<SeckillResultVO>(uri, undefined, {
    headers: generateReplayHeaders(uri)
  })
}

/** 一键执行秒杀（带防重放签名） */
export function doSeckill(
  seckillId: number | string,
  seckillToken: string
): Promise<Result<SeckillResultVO>> {
  const uri = `/api/v1/seckill/${seckillId}`
  return post<SeckillResultVO>(uri, undefined, {
    params: { seckillToken },
    headers: {
      'X-Seckill-Token': seckillToken,
      ...generateReplayHeaders(uri)
    }
  })
}
```

⚠️ 安全注意：签名密钥需前端和后端共享。在前端代码中暴露密钥有安全风险，但这是防重放（防机器人刷接口）而非防破解，在秒杀场景下是可接受的折中方案。生产环境建议通过构建时环境变量注入。

#### 5.2 后端 CORS 头修复

修改 `ReplayProtectionFilter.java` 的 `reject()` 方法：

```java
@Value("${seckill.security.cors.allowed-origins:http://localhost:5173,http://localhost:8080,http://127.0.0.1:5173,http://192.168.176.71:5173}")
private String corsAllowedOrigins;

private void reject(HttpServletRequest request, HttpServletResponse response, ErrorCode errorCode) throws IOException {
    // 补充 CORS 头：因为本 Filter 在 Spring Security 之前执行，
    // reject 时 CorsFilter 尚未添加 CORS 头，需手动补充
    String origin = request.getHeader("Origin");
    if (origin != null) {
        List<String> allowedOrigins = Arrays.asList(corsAllowedOrigins.split(","));
        if (allowedOrigins.contains(origin)) {
            response.setHeader("Access-Control-Allow-Origin", origin);
            response.setHeader("Access-Control-Allow-Credentials", "true");
            response.setHeader("Access-Control-Allow-Methods", "GET,POST,PUT,DELETE,OPTIONS,PATCH");
            response.setHeader("Access-Control-Allow-Headers", "*");
            response.setHeader("Access-Control-Max-Age", "3600");
        }
    }
    response.setStatus(HttpStatus.UNAUTHORIZED.value());
    response.setContentType(MediaType.APPLICATION_JSON_VALUE);
    response.setCharacterEncoding(StandardCharsets.UTF_8.name());
    response.getWriter().write(objectMapper.writeValueAsString(Result.error(errorCode)));
}
```

#### 5.3 Vite 代理修复

修改 `frontend/.env.development`：

```diff
- VITE_API_BASE_URL=http://localhost:8080
+ VITE_API_BASE_URL=
```

这样所有 API 请求走相对路径，由 Vite 代理转发到后端，开发时无跨域问题。

---

### 方案B：仅修复前端

| 层次 | 修复内容 | 说明 |
|------|---------|------|
| 前端-防重放签名 | ✅ 必须修 | 不加签名头，后端永远返回 401 |
| 前端-Vite代理 | ✅ 建议修 | 不改代理，开发时 CORS 问题仍可能复现 |

后端不改的话，ReplayProtectionFilter 的 reject 响应仍缺 CORS 头。如果同时改用 Vite 代理则开发时无跨域问题可规避；但如果仍用绝对 URL，其他触发 reject 的场景（如签名错误）仍会出现"网络错误"。

---

### 方案C：不修复

仅作为问题排查参考，不修改任何代码。

---

## 六、方案对比

| | 方案A（全部修复） | 方案B（仅前端） | 方案C（不修复） |
|---|---|---|---|
| 防重放签名 | ✅ | ✅ | ❌ |
| 后端CORS头 | ✅ | ❌（有隐患） | ❌ |
| Vite代理 | ✅ | ✅（建议） | ❌ |
| 开发环境稳定 | ⭐⭐⭐ | ⭐⭐ | ⭐ |
| 生产环境稳定 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐ |
| 改动量 | 大 | 中 | 无 |
| 推荐度 | **推荐** | 可行 | 仅排查 |

---

## 七、Vite 代理 vs Nginx 反向代理

### 7.1 本质相同，场景不同

两者都是**反向代理**，核心原理一致：客户端请求代理服务器，代理服务器转发到后端，对客户端隐藏了后端地址。

### 7.2 关键区别

| 对比项 | Vite 代理 | Nginx 反向代理 |
|--------|-----------|---------------|
| 使用场景 | **仅开发环境**（`npm run dev`） | **生产环境**（部署上线） |
| 运行位置 | Vite 开发服务器内部（Node.js 进程） | 独立的 Nginx 服务进程 |
| 配置位置 | `vite.config.ts` 的 `server.proxy` | `nginx.conf` |
| 是否需要额外安装 | 不需要（Vite 内置） | 需要安装 Nginx |
| 性能 | 低（Node.js 单线程，仅开发用） | 高（C 语言实现，事件驱动） |
| 功能 | 简单转发 | 负载均衡、缓存、SSL、限流等 |
| 生命周期 | 随 `npm run dev` 启停 | 随服务器常驻运行 |
| 生产环境是否可用 | ❌ 不可（Vite 仅开发工具） | ✅ 生产标配 |

### 7.3 为什么两个都需要？

```
开发阶段：
  浏览器 → Vite代理(5173) → 后端(8080)
  （Vite 代理解决开发时的跨域问题，无需额外配置 Nginx）

生产阶段：
  浏览器 → Nginx(80) → 后端(8080)
  （Nginx 处理静态资源、SSL、负载均衡、反向代理等）
```

**Vite 代理是开发时的便利工具，Nginx 是生产部署的必要组件，两者互补而非互斥。**

### 7.4 当前项目情况

当前项目 `docker-compose.yml` 中**没有 Nginx 服务**，只有 MySQL、Redis、RabbitMQ 和 Spring Boot。这意味着：
- 开发环境：依赖 Vite 代理（但当前未启用，因为 `VITE_API_BASE_URL` 是绝对 URL）
- 生产环境：缺少 Nginx 做反向代理和静态资源服务，建议后续补充

---

## 八、补充建议

1. **生产环境添加 Nginx**：在 `docker-compose.yml` 中增加 Nginx 服务，负责：
   - 静态资源服务（前端 dist 包）
   - API 反向代理（`/api` → 后端 8080）
   - SSL 终止
   - 限流保护

2. **签名密钥管理**：生产环境不要在前端代码中硬编码签名密钥，建议通过构建时环境变量注入（`VITE_SIGN_SECRET`），CI/CD 流水线从密钥管理服务获取。

3. **ReplayProtectionFilter 优化**：考虑将 Filter 的 Order 调整到 Spring Security 过滤链之后，或改用 Spring Security 的 `Handler` 机制处理拒绝逻辑，确保 CORS 头始终正确添加。